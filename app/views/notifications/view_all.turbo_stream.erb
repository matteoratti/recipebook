<%= turbo_stream.replace nested_dom_id('notification-view-all') do %>
  All notifications marked as viewed
<% end %>

<% @notifications.each do |notification| %>
  <%= turbo_stream.replace nested_dom_id('notification-view-icon', notification.id) do %>
    <i class="bi bi-bookmark-check"></i>
  <% end %>
<% end %>

